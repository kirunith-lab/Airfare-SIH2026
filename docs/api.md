# API

- `GET /api/health` checks service availability.
- `GET /api/routes` lists configured routes.
- `GET /api/index/{origin}/{destination}` returns route price history and index metrics.
- `GET /api/analytics/summary` returns dashboard counters.
